{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "unset VIRTUAL_ENV; export PYTHONWARNINGS=ignore::SyntaxWarning; GM=\"${CLAUDE_PLUGIN_ROOT}/skills/mythras-gm\"; [ -d \"$GM\" ] || GM=\"${CLAUDE_PLUGIN_ROOT}\"; uv run --project \"$GM\" python \"$GM/mythras_init.py\"",
            "timeout": 90000
          }
        ]
      }
    ]
  }
}
